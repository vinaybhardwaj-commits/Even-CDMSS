# dependencies
/node_modules
# next.js
/.next
/out
# env
.env
.env*.local
# misc
.DS_Store
*.pem
npm-debug.log*
/next-env.d.ts

# icloud/fuse artifacts
.fuse_hidden*
**/.fuse_hidden*
tsconfig.tsbuildinfo
.vercel
.env*

# local result packs / study artifacts / dumps — NEVER commit (public repo; may carry
# de-identified-but-clinical extracts + link-back keys). Committed data lives in data/ and does
# NOT match these (e.g. data/ipd-audit-gold.json, data/llm-pricing.json are safe).
.study-tb/
OPD-Audit-v0.5-dump*
*-export-*.csv
consensus-gold-*.json
ipd-audit-s4-*.json
ipd-batch-*.json
*.cache.json
run1-results.json
real-order-probe-*.json
episode-recon-*.json
# the frozen recon-fidelity gold IS committed (SL5b), like data/ipd-audit-gold.json
!data/episode-recon-gold.json

# --- housekeeping 18 Jul 2026: strays, local settings, data packs, CC build reports ---
# local PHI-dump helper (runs locally where PHI access is sanctioned; infra URLs inside)
opd_audit_cohort_dump.py
# local editor/agent settings
.claude/settings.local.json
# stray iCloud-synced nested copy that leaked into the tree
/Daily Dash EHRC/
# eval / sample / mini-pipeline result packs (may carry clinical extracts)
/data/inquiry-eval/
ipd-audit-m0-sample-pack.*
ipd-mini-stage1.json
# CC build-report markdown (de-identified clinical finding text) — kickoffs/PRDs (CDMSS-*.md) stay untracked, NOT ignored
adjudication-ledger*.md
consensus-gold-2.0.md
consensus-gold-harness.md
cost-column-backfill.md
cost-tracker-accuracy-fix.md
episode-state-sl*.md
memberstate-adapter-sl*.md
ipd-audit-flash-tiering-study.md
ipd-audit-thinkingbudget-study.md
ipd-audit-s4-k5-v2.md
ipd-audit-s4-theme-rescore*.md

# Brainstem PR 0 local eval pack (de-identified clinical excerpts + link-back keys) — never commit
.corpus-eval/
